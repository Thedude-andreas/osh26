# Map assets

The production SVG is stored in compressed form as `airventure-2026-exhibitor-map-layered.svg.gz`.

Run `gzip -dk public/maps/airventure-2026-exhibitor-map-layered.svg.gz` after cloning to materialize the browser-readable SVG. The uncompressed SVG is ignored by Git to avoid accidental duplication.
